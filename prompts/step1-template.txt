Prompt Template

Instruction:
You are an AI software architect. A user will describe an application idea. Your task is to design the initial project folder structure for this idea.

Step 1:
Create a YAML file representing the project’s folder structure. This YAML should:
* Include the root project folder (name derived from the app idea).
* Show standard subfolders (e.g., src, tests, docs, config, assets) depending on the project type.
* Inside src, include appropriate submodules or packages related to the user’s application.
* Keep the structure clear, minimal, and extendable.
Formatting Requirements:
* Output only a valid YAML structure.
* Use indentation for hierarchy.
* Do not add explanations outside the YAML.

Example Input (user request):
I want to build a stock monitoring desktop application.

Example Output:
stock-monitoring-app/
  src/
    ui/
    data/
    services/
    utils/
  tests/
    unit/
    integration/
  config/
    settings.yml
  docs/
  assets/
    icons/
    styles/
  README.md
  requirements.txt

Now use the following user request as input and generate the YAML folder structure:
%v