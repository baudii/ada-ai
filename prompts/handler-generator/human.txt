You will receive:

1. A Go file contents that contains a comment that describes the file, and go code including: package, imports, a handler function with a placeholder comment.
The file will be wrapped like this:

--- HANDLER_FILE_START ---
<full Go file with handler and placeholder>
--- HANDLER_FILE_END ---

2. An "Existing interfaces context" section, which contains Go code with previously defined interfaces and their methods, including comments describing each interface and method.
This context represents dependencies that already exist in the project and may be reused or extended.
The context will be wrapped like this:

--- EXISTING_INTERFACES_START ---
<Go code with existing interfaces and comments, or 'none'>
--- EXISTING_INTERFACES_END ---

Your task:

* Generate ONLY the body of the handler function (the code that replaces the placeholder comment).
* Do NOT output the function signature.
* Do NOT output unrelated code.
* Focus only on the logic required inside the handler.

When the handler needs to call data storage, business logic, external systems, or any other dependency, you must decide how to use interfaces based on the "Existing interfaces context":

You must choose one of the following:

1. Use an existing interface and an existing method from the context.
2. Use an existing interface from the context but add a NEW method to it.
3. Define a completely NEW interface if nothing appropriate exists.

Assume that any interface you use is (or will be) available as a field on the `Server` struct (e.g., repositories, services, providers, clients, etc.).

If you introduce a new interface or add new methods to an existing interface, you must:

* Provide the full interface definition in the `## interfaces` section.
* Provide a structured description in the `## interfaces_description` section.

Do NOT repeat unchanged interfaces from the context in the `## interfaces` section. Only include:

* completely new interfaces, or
* existing interfaces that you are extending with new methods.

Your output must contain EXACTLY three sections, in this order:

1. ## function
2. ## interfaces
3. ## interfaces_description

### Section: ## function

This section must contain a Go code block with ONLY the body of the handler function, without the function signature.

Example structure (placeholder):

```go
ctx := r.Context()

addresses, err := s.AddressRepository.GetAddresses(ctx)
if err != nil {
    http.Error(w, "failed to load addresses", http.StatusInternalServerError)
    return
}

w.Header().Set("Content-Type", "application/json")
if err := json.NewEncoder(w).Encode(addresses); err != nil {
    http.Error(w, "failed to write response", http.StatusInternalServerError)
    return
}
```

### Section: ## interfaces

If you introduced any new interfaces or extended existing ones with new methods, list EACH such interface here in a single Go code block, with the full up-to-date interface definition.

Example:

```go
type AddressRepository interface {
    GetAddresses(ctx context.Context) ([]Address, error)
    CreateAddress(ctx context.Context, addr Address) error
}
```

If you did NOT introduce or change any interfaces, output exactly:

```
none
```

### Section: ## interfaces_description

Provide a structured, machine-readable description for EVERY interface that appears in the `## interfaces` section.

For each interface, use the following format:

```
Interface: <Name>
Description: <description of what this interface represents and why it is needed>

Methods:
  - <MethodName>
    Signature: <full Go method signature>
    Description: <what the method does and why it is needed>
```

If you did NOT introduce or change any interfaces (i.e., `## interfaces` is `none`), then output exactly:

```
none
```

### Rules

* Output ONLY the three required sections in the exact order:

  * ## function
  * ## interfaces
  * ## interfaces_description
* Never include the function signature in `## function` — only the body.
* All code must be valid, idiomatic Go. Imports will be handled separately.
* Use the "Existing interfaces context" to avoid redefining interfaces that already exist.
* Prefer reusing and extending existing interfaces over creating new ones when it makes sense.

--- HANDLER_FILE_START ---
%v
--- HANDLER_FILE_END ---

--- EXISTING_INTERFACES_START ---
%v
--- EXISTING_INTERFACES_END ---